import { HealthTipsPipePipe } from './health-tips-pipe.pipe';

describe('HealthTipsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HealthTipsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
